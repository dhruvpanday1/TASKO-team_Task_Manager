{
  "name": "tasko-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "npx prisma generate && npx prisma migrate deploy",
    "start": "node --import tsx/esm src/app.ts",
    "dev": "tsx watch src/app.ts"
  }
}
